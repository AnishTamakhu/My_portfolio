# My portfolio

This is a static HTML, CSS, and JavaScript portfolio hosted with GitHub Pages.

## Hosting

Configure GitHub Pages to publish the repository root and set the custom domain to `anishtamakhu.com.np`.

The contact form opens the visitor's email client and does not require a Node.js server.
